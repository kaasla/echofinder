apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: echofinder
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: echofinder
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
